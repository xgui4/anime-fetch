#include <string>

struct MemoryInfo {
    std::string vendor_name;
    std::string model_name; 
    std::string name; 
    std::string serial; 
    std::string total_memory; 
    std::string free_memory; 
};